name: shortuid
description: Generate UUIDs or short uuids (32/64 bit Guid), RFC4122 v4-compliant UUIDs 
version: 1.0.1
repository: https://github.com/parz3val/dart-shortid

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  uuid: ^4.2.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
