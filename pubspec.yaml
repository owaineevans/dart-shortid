name: shortuid
description: Generate UUIDs or short uuids (32/64 bit Guid), RFC4122 v4-compliant UUIDs 
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  uuid: ^3.0.7
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
